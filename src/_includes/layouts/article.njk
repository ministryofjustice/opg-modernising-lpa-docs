---
layout: layouts/fullwidth.njk
backLink: "/research-development/"
---

<h1 class="govuk-heading-xl">{{title}}</h1>

{%- from "../../_includes/partials/contentMetaData.njk" import contentMetaData %}
{{ contentMetaData(date, contributors) }}
{{ content | toc | safe }}
{%- include "../partials/githubHistory.njk" -%}

{{ content | safe }}
